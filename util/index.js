const clientSymbol = Symbol('osc-client');
const messageSymbol = Symbol('osc-message');
const serverSymbol = Symbol('osc-server');

module.exports = {
  clientSymbol,
  messageSymbol,
  serverSymbol
};
