# Use package-lock.json for exact dependency versions
package-lock=true

# Save exact versions in package.json
save-exact=false

# Prefer using npm ci in CI environments
# Run 'npm ci' instead of 'npm install' for reproducible builds
